language: cpp
compiler: gcc
sudo: false
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
      - gfortran
      - libbz2-dev
      - libblas-dev
      - liblapack-dev
      - libarpack2-dev
      - libnetcdf-dev
      - netcdf-bin
env:
    - COMPILER=gnu
    - COMPILER=gnu OPENMP="-openmp" OPT=openmp

install:
    - ./configure $OPENMP $COMPILER
    - make -j2 install
script:
    - cd test && make test
